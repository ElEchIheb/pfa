<form method="post" action="{{ path('app_option_delete', {'id': option.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cet option ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
