{% extends 'base.html.twig' %}

{% block title %}ProfilposteResultat{% endblock %}

{% block body %}
    <h1>ProfilposteResultat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profilposte_resultat.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ profilposte_resultat.score }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ profilposte_resultat.resultat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profilposte_resultat_index') }}">back to list</a>

    <a href="{{ path('app_profilposte_resultat_edit', {'id': profilposte_resultat.id}) }}">edit</a>

    {{ include('profilposte_resultat/_delete_form.html.twig') }}
{% endblock %}
